export enum AppRoute {
  Main = '/',
  Lobby = '/lobby',
  Profile = '/profile',
  Game = '/game',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
