export type GameData = {
  id: string,
  name: string,
  users: number,
  maxUsers: number,
  isPrivate: boolean,
}


export type MinGameData = {
  id: string,
  name: string,
  isPrivate: boolean,
}
